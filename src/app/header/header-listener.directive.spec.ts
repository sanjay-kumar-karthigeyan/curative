import { HeaderListenerDirective } from './header-listener.directive';

describe('HeaderListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderListenerDirective();
    expect(directive).toBeTruthy();
  });
});
