import { RehabilationBodyDirective } from './rehabilation-body.directive';

describe('RehabilationBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new RehabilationBodyDirective();
    expect(directive).toBeTruthy();
  });
});
